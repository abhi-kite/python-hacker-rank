if __name__ == '__main__':
    n = int(raw_input())
    arr = map(int, raw_input().split())
    
    lis=list(arr)
    z = max(lis)
    while max(lis) == z:
        lis.remove(max(lis))
    print max(lis)
